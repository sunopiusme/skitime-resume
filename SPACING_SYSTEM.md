# Система отступов Composer

## Философия

Отступы должны создавать чёткую визуальную иерархию и давать контенту "дышать", особенно на маленьких экранах. Используем модульную шкалу с коэффициентом 1.5 (идеальная квинта) — проверенная музыкальная пропорция.

## Модульная шкала (базовая единица: 8px)

```
Уровень 0: 8px   (base unit)
Уровень 1: 12px  (8 × 1.5)
Уровень 2: 18px  (12 × 1.5)
Уровень 3: 27px  (18 × 1.5)
Уровень 4: 40px  (27 × 1.5, округлено)
Уровень 5: 60px  (40 × 1.5)
Уровень 6: 90px  (60 × 1.5)
Уровень 7: 135px (90 × 1.5)
Уровень 8: 200px (135 × 1.5, округлено)
```

## Применение в Composer

### 1. Внутри элементов (micro spacing)
- **prose gap**: 24px → 32px (уровень 4)
  - Между параграфами нужно больше воздуха
  
- **hero internal gap**: 32px → 48px (между уровнями 4-5)
  - Внутри hero больше пространства для дыхания

### 2. Между элементами секции (meso spacing)
- **part gap** (внутри секции): 48px → 64px → 96px
  - clamp(48px, 5vw, 96px)
  - Между фигурами и текстом в одной секции

### 3. Между секциями (macro spacing)
- **section gap** (между секциями): 96px → 128px → 160px
  - clamp(96px, 10vw, 160px)
  - Чёткое разделение смысловых блоков

### 4. Специальные случаи
- **hero padding**: 64px → 96px → 128px
  - clamp(64px, 8vw, 128px)
  - Hero получает максимум пространства

## Новые токены

```css
/* Базовая единица */
--composer-unit: 8px;

/* Модульная шкала spacing */
--composer-space-1: 12px;
--composer-space-2: 18px;
--composer-space-3: 27px;
--composer-space-4: 40px;
--composer-space-5: 60px;
--composer-space-6: 90px;
--composer-space-7: 135px;
--composer-space-8: 200px;

/* Адаптивные отступы (responsive) */
--composer-space-prose: clamp(24px, 2.5vw, 32px);
--composer-space-section-inner: clamp(48px, 5vw, 96px);
--composer-space-section-outer: clamp(96px, 10vw, 160px);
--composer-space-hero: clamp(64px, 8vw, 128px);
```

## Сравнение: До и После

### Маленький экран (375px)
**До:**
- Между секциями: 72px (слишком много для маленького экрана)
- Внутри секции: 32px (мало)
- Prose gap: 20px (мало)

**После:**
- Между секциями: 96px (больше воздуха)
- Внутри секции: 48px (комфортнее)
- Prose gap: 24px (лучше читается)

### Средний экран (768px)
**До:**
- Между секциями: ~85px
- Внутри секции: ~40px
- Prose gap: 20px

**После:**
- Между секциями: ~120px (пропорционально больше)
- Внутри секции: ~64px (золотая середина)
- Prose gap: ~28px (комфортное чтение)

### Большой экран (1440px+)
**До:**
- Между секциями: 112px
- Внутри секции: 56px
- Prose gap: 20px

**После:**
- Между секциями: 160px (максимальный воздух)
- Внутри секции: 96px (просторно)
- Prose gap: 32px (идеально для чтения)

## Принципы

1. **Больше воздуха на всех размерах экрана**
   - Минимальные значения увеличены на 20-30%
   
2. **Чёткая иерархия**
   - Prose (24-32px) < Section inner (48-96px) < Section outer (96-160px)
   
3. **Математическая прогрессия**
   - Каждый уровень в 1.5 раза больше предыдущего
   
4. **Адаптивность**
   - clamp() обеспечивает плавный переход между размерами

## Реализация

1. Обновить `src/design-system/composer.ts`
2. Обновить `src/features/projects/composer/case.module.css`
3. Проверить на всех размерах экрана (375px, 768px, 1440px)
